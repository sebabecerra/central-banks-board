# Central Bank Governors Map

## Run

1. Install dependencies:

```bash
npm install
```

2. Start the app:

```bash
npm run dev
```

The app reads `public/governors_by_country.json`, which is generated from `data/kof_governors_with_sources.csv`.
